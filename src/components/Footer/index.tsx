import { FC } from "react"
import s from "./Footer.module.scss"

export const Footer: FC = () => {
    return (
        <footer className={s.footer}>
            <div>Footer</div>
        </footer>
    )
}